{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run prisma:generate && npm run build --workspace @elite-dev/api"
  },
  "deploy": {
    "preDeployCommand": ["npm run db:migrate:deploy"],
    "startCommand": "npm run start --workspace @elite-dev/api",
    "healthcheckPath": "/health"
  }
}
